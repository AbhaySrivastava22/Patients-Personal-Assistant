<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytherapy">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".nearbypharmacy1"></activity>
        <activity android:name=".nearbydoctor1" />
        <activity android:name=".searchocrimage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".nearbydc"
            android:label="@string/title_activity_nearbydc" />
        <activity android:name=".searchocr" />
        <activity android:name=".searchdisease" />
        <activity
            android:name=".nearbypharmacy"
            android:label="@string/title_activity_nearbypharmacy" />
        <activity
            android:name=".nearbydoctor"
            android:label="@string/title_activity_nearbydoctor" />
        <activity android:name=".reminder" />
        <activity android:name=".addmedicine" />
        <activity android:name=".doctorsappointment" />
        <activity android:name=".pharmacydashboard" />
        <activity android:name=".doctordashboard" />
        <activity android:name=".searchmedicine" />
        <activity android:name=".pharmacy" />
        <activity android:name=".doctor" />
        <activity android:name=".Report" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".userprofile" />
        <activity android:name=".pharmacysignup" />
        <activity android:name=".patientsignup" />
        <activity android:name=".doctorsignup" />
        <activity android:name=".dashboard" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Feedback" />
        <activity android:name=".AboutUs" />
        <activity android:name=".welcomescreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/AppTheme" />
        <receiver android:name=".AlarmReciver"/>
    </application>

</manifest>